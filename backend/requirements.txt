fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
GeoAlchemy2==0.16.0
pgvector==0.3.6
celery[redis]==5.4.0
redis==5.2.1
argon2-cffi==23.1.0
PyJWT==2.10.1
python-multipart==0.0.20
httpx==0.28.1
structlog==24.4.0
astral==3.2
# AI detection: MegaDetector v6 loaded directly via Ultralytics (no PytorchWildlife —
# it drags in unused librosa/torchaudio). torch/torchvision installed (CPU) in the Dockerfile.
ultralytics==8.4.66
timm
# Cloud vision for sex/class (stag vs hind, boar sex) — opt-in, needs ANTHROPIC_API_KEY.
anthropic==0.69.0
# Encrypt guest SPYPOINT passwords at rest (Fernet, key derived from JWT_SECRET).
cryptography==44.0.0
