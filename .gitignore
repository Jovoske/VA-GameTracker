# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
.env.local
.env.*.local

# Data / media / model weights (never committed)
/data/
media/
models/
*.db
*.sqlite3
*.pt
*.pth
*.onnx

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Runtime / local artifacts
backend/celerybeat-schedule*
.claude/settings.local.json
_*.py

# Installed agent skills. The files are fetched, not authored here, and
# .claude/skills holds NTFS junctions that git would follow and commit twice.
# skills-lock.json is tracked so the set stays reproducible.
.agents/
.claude/skills/
